import React from 'react'

import './not-allowed.scss'

export const NotAllowed = (/* props */) => (
  <div className="app-not-allowed-container">
    <div className="app-not-allowed">:(</div>
  </div>
)

export default NotAllowed
